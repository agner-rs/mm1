[package]
name = "mm1-tests"
version = "0.1.0"
edition = "2024"

[dependencies]
[dev-dependencies]
mm1 = { workspace = true, features = ["message-format-rmp"] }
mm1-logger.workspace = true

serde = { workspace = true, features = ["derive"] }
serde_yaml.workspace = true
tokio = { workspace = true, features = ["macros", "rt"] }
